import React from "react";
import "./Loader.css"; // Estilos que definiremos a continuación

const Loader = () => {
  return (
    <div className="loader-container">
      <div className="loader"></div>
    </div>
  );
};

export default Loader;
